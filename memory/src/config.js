import { CANVAS, } from "phaser"

export default {
   type: CANVAS,
   width: 1280,
   height: 720,
   rows: 2,
   cols: 5,
   cardWidth: 180,
   cardHeight: 260,
   idCards: [1, 2, 3, 4, 5,],
   timeout: 20,
}